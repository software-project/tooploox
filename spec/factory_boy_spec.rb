require "spec_helper"

describe FactoryBoy do
  it "has a version number" do
    expect(FactoryBoy::VERSION).not_to be nil
  end

  describe '#define_factory'
  it "allows to define" do
    expect(false).to eq(true)
  end
end
